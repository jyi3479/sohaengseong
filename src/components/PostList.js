import React from "react";
import styled from "styled-components";

const PostList = (props) => {
  return <Wrap>인증 리스트</Wrap>;
};

const Wrap = styled.div`
  padding: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
`;

export default PostList;
